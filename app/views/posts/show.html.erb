<h1>Posts#show</h1>
<h1>Pictures#index</h1>
<%= link_to "写真追加", new_post_path %>

<% if current_field.posts.present? %>
  <% @posts.each do |post| %>
    <% post.pictures.each do |picture| %>
      <%= attachment_image_tag picture, :image, class: "default_image" %>
    <% end %>
  <% end %>
<% else %>
  投稿写真はありません。
<% end %>
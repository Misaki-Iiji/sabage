<h1>Posts#show</h1>
<h1>Pictures#index</h1>
<%= link_to "写真追加", new_post_path %>

<% if current_field.posts.present? %>
  <% @posts.each do |pict| %>
    <%= attachment_image_tag pict, :picture, class: "default_image" %>
  <% end %>
<% else %>
  投稿写真はありません。
<% end %>
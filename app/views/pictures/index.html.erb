<h1>Pictures#index</h1>
<%= link_to "写真追加", new_field_path %>

<%# if @field.pictures.present? %>
  <% @pictures.each do |pict| %>
    <%= attachment_image_tag pict, :picture, class: "default_image" %>
  <% end %>
<%# else %>
  投稿写真はありません。
<% #end %>
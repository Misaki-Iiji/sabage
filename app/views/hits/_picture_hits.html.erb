<% if user_signed_in? %><!--ユーザーログイン時のみ表示(フィールドはいいねできない)-->
  <% if picture.favorited_by?(current_user) %>
    <p>
      <%= link_to post_picture_hit_path(picture), method: :delete do %>
        ♥<%= picture.picture_hits.count %> いいね
      <% end %>
    </p>
    <% else %>
    <p>
      <%= link_to post_picture_hit_path(picture), method: :post do %>
        ♡<%= picture.picture_hits.count %> いいね
      <% end %>
    </p>
  <% end %>
<% end %>


<% if field_signed_in? %>
  (お気に入り<%= picture.pictures.count %>件)
<% end %>
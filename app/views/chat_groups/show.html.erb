<h2><%= @chat_group.chat_group_name %></h2>

<%# @users.each do |user| %>
  <%# member.user.name %>
  <%# link_to user_path(member) do %>
    <%# attachment_image_tag member, :image %>
  <%# end %>
<%# end %>

<%= link_to "編集", edit_chat_group_path %>
<%= link_to "削除", chat_group_path(@chat_group), method: :delete, data: { confirm: 'グループ削除しますか?' } %>
<% if @chat_group.chat_group_description != "" %>
  <%= @chat_group.chat_group_description %>
<% else %>
  説明文はありません。
<% end %>

<div class="mx-auto">
  <div class="container message">

    <h3 class="text-left border-bottom m-3"><%= @user.nickname %></h3>
    <% @chats.each do |chat| %>
      <% if chat.user_id == current_user.id %>
        <p class="text-right mr-3"><%= chat.message %></p>
      <% else %>
        <p class="text-left ml-3"><%= chat.message %></p>
      <% end %>
    <% end %>
  </div>

    <div class="text-right message_box">
      <%= form_with model: @chat do |f| %>
        <%= f.text_field :message %>
        <%= f.hidden_field :room_id %>
      <% end %>
    </div>
</div>
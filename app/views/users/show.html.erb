<div class="mx-auto" style="width: 900px;">
  <div class="text-center">

    <h2>HOME</h2>
    <% if flash[:notice] %>
      <p><%= flash[:notice] %></p>
    <% end %>
  </div>
</div>

      <%= link_to image_tag("icon5.png", :size => "25x25"), edit_user_path(current_user), class:"m-2" %>
      <%= render 'relationships/follow_button', user: @user %>
      <%# if current_user.following?(info) %>
        <% if current_user != @user %>
          <%= link_to image_tag("icon8.png", :size => "25x25"), chat_path(@user.id) %>
        <% end %>
      <%# end %>


<div class="container">
  <h3 class="text-center">お気に入りフィールド</h3>
<div class="row">
  <% @fields.each do |field| %>
    <div class="col-xs-3">
      <div class="card m-1">
        <div class="card-header">
          <%= attachment_image_tag field, :image, :size => '160x100', fallback: "no_image.jpg" %>
        </div>
        <div class="card-body">
         <%= link_to field.name, field_path(field) %>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>

<div class="container">
  <h3 class="text-center">いいね登録写真</h3>
  <div class="row">
    <% @pictures.each do |picture| %>
      <%= attachment_image_tag picture, :image, :size => '150x150', class: "m-1" %>
    <% end %>
  </div>
</div>